# 📈 Stock Pulse - 台股成交值排行儀表板

一個專注於視覺化呈現台股（上市/上櫃）每日成交值排行的儀表板。透過直觀的 K 線圖與 30 日趨勢圖，幫助使用者一眼掌握市場熱門資金流向。

![Dashboard Preview](https://via.placeholder.com/1200x600?text=Stock+Pulse+Dashboard+Preview)

## ✨ 特色功能

*   **雙市場整合**：一鍵切換「上市公司 (TWSE)」與「上櫃公司 (TPEx)」成交值排行。
*   **視覺化圖表**：
    *   **10 日 K 線圖**：包含開高低收 (OHLC) 資訊，紅漲綠跌，均線輔助。
    *   **30 日趨勢圖**：呈現近一個月的股價走勢，快速判斷多空趨勢。
*   **智慧刻度**：Y 軸刻度自動計算最高、最低與**精確平均值**（百元以上四捨五入，百元以下保留一位小數），並輔以格線，價格區間一目瞭然。
*   **即時/歷史數據**：
    *   支援顯示最近一個交易日的完整數據。
    *   歷史數據自動存檔，長期追蹤。
*   **自動化資料管線**：整合 GitHub Actions 與 Supabase，每日盤後自動爬取最新數據。

## 🛠 技術架構

*   **前端**：React, Vite, Tailwind CSS, Recharts
*   **後端**：Node.js, Express
*   **資料庫**：PostgreSQL (Supabase) / SQLite (本地開發)
*   **部署與維運**：GitHub Actions (CI/CD & Cron Jobs)

## 🚀 快速開始

### 1. 安裝依賴
```bash
npm install
```

### 2. 環境設定
專案支援由 `db_adapter.cjs` 自動切換資料庫模式：
*   **本地開發 (預設)**：無需設定，自動使用本地 `server/transactions.db` (SQLite)。
*   **Production**：若設定環境變數 `DATABASE_URL`，則自動連線至 PostgreSQL。

建立 `.env` 檔案 (可選)：
```env
# 若要測試連線 PostgreSQL
DATABASE_URL=postgres://user:pass@host:port/dbname
```

### 3. 啟動開發伺服器
```bash
# 同時啟動前端 (Vite) 與後端 (Express)
npm run dev
```
前往 `http://localhost:5173` 即可看到畫面。

### 4. 手動抓取資料 (測試用)
```bash
node server/scripts/fetch_twse.cjs
# 預設抓取「昨日」數據。也可指定日期：
# node server/scripts/fetch_twse.cjs 2024-12-31
```

## ⚙️ 自動化部署與更新

本專案已設定 GitHub Actions workflow (`.github/workflows/daily-update.yml`)。

*   **觸發時間**：每天台灣時間下午 3:00 (UTC 07:00)。
*   **執行內容**：
    1.  自動執行爬蟲腳本。
    2.  將最新成交值前 100 名及個股歷史數據寫入 Supabase 資料庫。
*   **設定需求**：
    至 GitHub Repo 的 `Settings > Secrets > Actions` 新增 `DATABASE_URL`，填入您的 PostgreSQL 連線字串。

## 📂 專案結構

```
.
├── src/                # React 前端原始碼
│   ├── app/            # 頁面與組件
│   └── ...
├── server/             # Node.js 後端 API
│   ├── db_adapter.cjs  # 雙模資料庫轉接層 (PG/SQLite)
│   ├── index.cjs       # Express 伺服器入口
│   └── scripts/        # 爬蟲與工具腳本
├── dist/               # 建置輸出
└── ecosystem.config.js # PM2 設定檔 (如有)
```

## 📝 License
MIT