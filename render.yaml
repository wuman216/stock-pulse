services:
  - type: web
    name: stock-pulse
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.cjs
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
